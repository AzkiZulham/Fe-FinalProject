export interface UserData {
  userName: string;
  email: string;
  avatar: string;
  phoneNumber: string;
  birthDate: string;
  gender: string;
  verified: boolean;
  isEmailUpdated: boolean;
  isEmailVerified?: boolean; 
}

